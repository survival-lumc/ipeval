% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ip_score_long.R
\name{add_lag_terms}
\alias{add_lag_terms}
\title{Add lagged versions of a longitudinal variable}
\usage{
add_lag_terms(df, var, lag = 1, fill = 0)
}
\arguments{
\item{df}{A long-format data frame containing an `id` column identifying
subjects.}

\item{var}{Character string giving the name of the variable to lag.}

\item{lag}{Integer vector specifying the lag(s) to create.}

\item{fill}{Value used when a lagged observation is unavailable (e.g. at the
first visit). Defaults to 0.}
}
\value{
The input data frame with additional columns named
  `(var)_lag_(lag)` containing the requested lagged values.
}
\description{
Creates one or more lagged versions of a variable in long-format data. Lagged
values are computed separately within each subject and added as new columns.
The data must be sorted by id and visit time before calling this function.
}
\examples{
df <- data.frame(
  id = c(1, 1, 1, 2, 2, 2),
  visit_time = c(0, 1, 2, 0, 1, 2),
  A = c(0, 1, 1, 1, 0, 0)
)

# Add a 1-visit lag of A
add_lag_terms(df, "A")

# Add both 1- and 2-visit lags
add_lag_terms(df, "A", lag = c(1, 2))
}
